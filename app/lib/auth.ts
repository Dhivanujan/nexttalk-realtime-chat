/**
 * Legacy file kept only to avoid editor errors after migrating to MERN.
 * Authentication now lives in server/src/routes/auth.routes.ts.
 */
export const authOptions = {
  deprecated: true,
  message: "NextAuth was removed during MERN migration.",
};
